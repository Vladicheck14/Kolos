import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  filterPaper: {
    padding: "1.2em 1.6em",
    marginBottom: "1em",
    position: "relative",
  },
  filterPaperNotExpanded: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
}));
